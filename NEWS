##########################################################
#      MISTRAL PROJECT - 2007 / 2008 /2009                          
#	  http://mistral.univ-avignon.fr
#								
# V1.35 / 30 March 2009
# Last edit / 30 March 2009
#
# Editor E.Charton / lia
# 
# Please visit:
# http://mistral.univ-avignon.fr
# http://alize.univ-avignon.fr
# http://mistral.univ-avignon.fr/wiki/
#
##########################################################

What's new !

1.35

	We had applied the LIUM Patches (N° p1 to p6) on this version. Diffs can be done trough the svn.
	Details follows:

	Add 2 fonctions in XList (p1)
    - addline(key, value) // one string 
    - seachValue(key)

	Add 1 fonction in Xline (p1)
    - create(key value)

	Patch LK Vector (p2)
	-Add copy constructor
	-add pack(capacity) to resize the vector to a capacity

	(p3)
	FrameAcc* 
    - new method: deaccumulate(frame)
    - new method: getDet()
	FrameAccG*
    - new method: add(FrameAccG*)
    - modif: comuputeAll() compute also the invert covariance, the det, the llh constant

	(p4)
	MixtureStat.*
	- computeAndAccumulateLLK(const Feature& f, const LKVector& topDistribsVector,
	    double w = 1.0f), compute and accumulate the llk using a top gaussian vector
	
	StatServer.*
	- deprecated some methods 
	- computeLLK given top gaussian vector

	(p5)
	add method setId(String) in Mixture

	(p6)
	- Corrected an enum: removed trailing ',' at the end
	- Changed the detection of LIUM segmentation file format 

	Detailed revision and discussions at http://mistral.univ-avignon.fr/wiki/index.php/Alize_1.35

1.33

	Library
	- Some modifications to permit compilation under Visual C++ 2005

1.32

	SPRO
	- Adaptation of Spro inclusion / Removed misc.c, spro.h and sig.c
	- Modification of FeatureComputer.cpp to include this mods
	- Modification of FeatureComputer.h
	- (all functions are renamed / objective is to avoid functions conflict)
	- Modification of compilation list / SRC/Makefile.am
	- Modification of alize.makefile
	
	
